certifi==2026.7.22
geopandas==1.1.4
iniconfig==2.3.0
numpy==2.5.2
packaging==26.3
pandas==3.0.5
pluggy==1.6.0
Pygments==2.21.0
pyogrio==0.13.0
pyproj==3.7.2
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
shapely==2.1.2
six==1.17.0

# User-facing app (Streamlit MVP). pydeck ships with Streamlit and is used
# directly for the map, so it is pinned explicitly. Their own dependencies
# (altair, pyarrow, tornado, ...) are resolved by pip.
streamlit==1.62.0
pydeck==0.9.3
